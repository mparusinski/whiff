# Whiff

`whiff` is a Rust reimplementation of `touch`.

## Development

```bash
git clone git@github.com:mparusinski/whiff.git

# Build
cd whiff
cargo build

# Run tests
cargo test

# Install
cargo install --path .
```
